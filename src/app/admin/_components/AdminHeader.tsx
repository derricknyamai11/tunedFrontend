"use client";
// AdminHeader kept for backward compatibility — functionality moved to AdminTopbar
import type { AuthUser } from "@/lib/types/auth.type";
export function AdminHeader({ user }: { user: AuthUser }) {
  return null;
}
